export interface ProfileData {
    id: number
    profilePicture: string
    firstName: string
    lastName: string
    age: number
    gender: string
    language: string[]
    address: string
    activities: number[]
}